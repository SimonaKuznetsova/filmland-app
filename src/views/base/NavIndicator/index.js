import React from 'react';

export const NavIndicator = ({styleWidth, styleLeft}) => {
    return (
        <div className='nav-indicator' style={{ width: styleWidth, left: styleLeft }}></div>
    )
}